spring.application.name=task-manager-ia

spring.datasource.url=${DB_URL:jdbc:postgresql://aws-0-ca-central-1.pooler.supabase.com:5432/postgres}
spring.datasource.username=${DB_USERNAME:postgres.zajtttdbzsholzjzbsfg}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

groq.api.key=${GROQ_API_KEY}
groq.api.url=https://api.groq.com/openai/v1/chat/completions

server.port=${PORT:8080}

jwt.secret=${JWT_SECRET:cambiame-por-un-secreto-largo-y-aleatorio-1234567890}
jwt.expiration=604800000

google.web.client-id=${GOOGLE_WEB_CLIENT_ID}
google.web.client-secret=${GOOGLE_WEB_CLIENT_SECRET}
google.web.redirect-uri=${GOOGLE_REDIRECT_URI}
